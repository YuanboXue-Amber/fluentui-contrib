import * as React from 'react';
import { render } from '@testing-library/react';
import { DndFlatTree } from './DndFlatTree';

describe('DndFlatTree', () => {
  it('should render', () => {
    render(<DndFlatTree />);
  });
});
